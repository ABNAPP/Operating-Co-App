# Engine Contracts (Placeholder)

## Intent
Define interfaces and data contracts between input/reference data and planned valuation engine stages.

## Planned Engine Contract List
1. Forecast & Fade Engine Contract
2. Reinvestment & FCFF Engine Contract
3. Risk/WACC Engine Contract
4. Terminal Value Engine Contract
5. Firm-to-Equity Bridge Engine Contract
6. Per-Share Engine Contract
7. Quality/Review Engine Contract
8. Decision Engine Contract

## Contract Rules
- Contracts are implemented in shared TypeScript modules under the Global Valuation Engine.
- Company-specific results store outputs generated by applying these shared contracts to one company.
- Dashboard consumes outputs but does not execute contracts or valuation logic.

## Phase 2 Coverage
- Added contract-ready type groups for:
  - Company identity and ticker logic
  - Currency structure and review status
  - Historical and forecast period keys
  - Company manual input sections
  - Reference data configuration sections
  - Valuation result placeholders by engine stage
  - Review severity flags and category statuses
  - Dashboard output row schema

## Phase 3 Coverage
- Added Firestore contract scaffolding for:
  - Company document persistence (`companies`)
  - Dashboard output row persistence (`dashboardRows`)
  - Shared reference data persistence collections
  - Build status persistence document
- Added repository function contracts for `get`, `upsert`, and `seed`
- Added fail-safe behavior to return mock fallback when Firestore is unavailable/empty

## Phase 4A Coverage
- Added daily refresh orchestration contract:
  - server-only cron trigger endpoint
  - `CRON_SECRET` authorization contract
  - refresh summary payload contract (`success`, timestamps, statuses, providers, warnings, errors)
- Added persistence contract for refresh status metadata:
  - source
  - last updated timestamps
  - provider/status fields
  - warning/error tracking
- Added explicit precedence contract:
  - Manual Override must always win over live value
- Added scheduling contract:
  - refresh runs daily via Vercel Cron at 06:00 UTC
  - refresh must not run on normal page loads
- Added corrected reference-data contracts:
  - Riskfree row contract with valuation-currency mapping fields
  - Currency map row contract for active/reporting/valuation/trading usage
  - FX pair row contract with separate from/to currencies
  - Stable ID contract (`riskfree_USD`, `currency_USD`, `fx_USD_EUR`, `fx_USD_USD`)
  - Same-currency FX selection contract (`selectedFxRate = 1`)

## Current Boundary (Still Not Implemented)
- No real valuation formulas
- No Firebase Admin SDK usage
- No live provider execution yet (implemented in Phase 4B)
- No runtime review engine execution (types only)
